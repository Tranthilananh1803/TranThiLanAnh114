@model IEnumerable<TranThiLanAnh114.Models.TTLA1114>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Thêm sinh viên</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TTLAName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TTLAGener)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TTLAName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TTLAGener)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TTLAId">Sửa</a> |
                <a asp-action="Details" asp-route-id="@item.TTLAId">Chi tiết</a> |
                <a asp-action="Delete" asp-route-id="@item.TTLAId">Xóa</a>
            </td>
        </tr>
}
    </tbody>
</table>
